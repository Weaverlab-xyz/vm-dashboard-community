# Create a PostgreSQL database.
# Target: a PostgreSQL database, provisioned or registered (Config Management → target
# kind "Databases"). The dashboard injects the admin connection as extra-vars —
# db_login_host / db_login_port / db_login_user / db_login_password — so you never
# type or see the credential. This is a localhost play: Ansible connects OUT to the
# DB endpoint — from the in-cloud runner for a cloud-hosted database, or from a sibling
# container on the dashboard host for an on-premises one.
#
# extra_vars:
#   target_db_name: appdb    # the database to create (required)
- name: Create a PostgreSQL database
  hosts: localhost
  connection: local
  gather_facts: false
  vars:
    target_db_name: ""
  tasks:
    - name: Validate required vars
      ansible.builtin.assert:
        that: target_db_name | length > 0
        fail_msg: "Set target_db_name via extra vars (e.g. target_db_name=appdb)."

    - name: Ensure the database exists
      community.postgresql.postgresql_db:
        name: "{{ target_db_name }}"
        state: present
        login_host: "{{ db_login_host }}"
        login_port: "{{ db_login_port }}"
        login_user: "{{ db_login_user }}"
        login_password: "{{ db_login_password }}"
      no_log: true   # the admin connection password is a secret
