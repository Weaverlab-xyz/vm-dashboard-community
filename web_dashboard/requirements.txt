# FastAPI and web server
fastapi==0.109.2
uvicorn[standard]==0.27.1

# Database
sqlalchemy==2.0.27
alembic==1.13.1
psycopg2-binary==2.9.9

# WSGI server (used in Docker / Container Apps)
gunicorn==21.2.0

# Data validation
pydantic>=2.7.2,<3.0.0
pydantic-settings>=2.6.1

# Authentication
python-jose[cryptography]==3.3.0
bcrypt==4.2.1
fido2>=1.1.2
msal>=1.28.0

# File uploads and forms
python-multipart==0.0.7

# Templates
jinja2==3.1.3

# Async file operations
aiofiles==23.2.1

# WebSockets
websockets==12.0

# Environment variables
python-dotenv==1.0.1

# Rate limiting
slowapi==0.1.9

# AWS SDK
boto3==1.34.69

# Azure SDK
# Upper bounds cap each package at the next major so a fresh build can't float into an
# untested SDK release. (azure-mgmt-resource 26.0.0 moved ResourceManagementClient out of
# the top-level namespace, which silently disabled the whole Azure integration on rebuild —
# see services/azure_service.py. Bump these deliberately when adopting a new major.)
azure-identity>=1.15.0,<2
azure-mgmt-compute>=30.0.0,<39
azure-mgmt-network>=25.0.0,<31
azure-mgmt-resource>=23.0.0,<27
azure-mgmt-containerinstance>=10.0.0,<11
azure-mgmt-storage>=21.0.0,<26
azure-keyvault-secrets>=4.7.0,<5
azure-mgmt-automation>=1.0.0,<2

# MCP (Model Context Protocol) server — exposes dashboard tools to AI clients
mcp>=1.2.0

# Google Cloud SDK
google-cloud-compute>=1.14.0
google-cloud-secret-manager>=2.20.0
google-cloud-storage>=2.14.0
google-cloud-run>=0.10.0
# google-cloud-build powers the GCP image-export Daisy workflow trigger in
# services/gcp_service.export_custom_image_to_vhd. Pulled in here rather
# than added to a runtime install path so it's part of the image's
# pre-resolved dependency set.
google-cloud-build>=3.20.0
google-auth>=2.20.0

# Azure Blob Storage (Packer template archive)
azure-storage-blob>=12.19.0,<13

# Async HTTP client (Portainer CE API, Ollama)
httpx>=0.27.0

# YAML parsing (playbook generation validation)
PyYAML>=6.0

# Proxmox VE REST API
proxmoxer>=2.0.0
requests>=2.31.0

# VMware vSphere / ESXi (vSphere Web Services API)
pyVmomi>=8.0.0

# Microsoft Hyper-V (WinRM remote PowerShell)
pywinrm>=0.4.3

# Required by Azure SDK async clients (azure-mgmt-automation.aio, azure-identity.aio)
aiohttp>=3.9.0

# SSH client (direct VM access, Portainer agent install)
# asyncssh is used instead of paramiko because paramiko 3+ dropped DSA key support,
# and BeyondTrust Password Safe releases DSS private keys for managed SSH accounts.
asyncssh>=2.14.0

# SMB / CIFS client — pure-Python, no system deps. Backs the local-storage
# UNC option on /storage so contributors can host playbooks on a corporate
# file share without bind-mounting it into the container.
smbprotocol>=1.12.0

# BeyondTrust Password Safe / Secrets Safe CLI (ps-cli)
beyondtrust-bips-cli
